# Default values for gatekeeper-rules.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nsExluded:
  - kube-system
  - gatekeeper-system
  - monitoring
  - local-path-storage
  - ingress-nginx
  - istio-operator
  - istio-system
  - exo1
  - exo3
  - exo4
  - exo5